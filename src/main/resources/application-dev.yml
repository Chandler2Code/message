spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/message?characterEncoding=utf-8&useSSL=false
    username: root
    password: root
  jpa:
    show-sql: true
server:
  port: 8081
  context-path: /serve
